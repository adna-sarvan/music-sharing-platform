{
  "name": "music-sharing-platform-backend",
  "version": "1.0.0",
  "description": "Backend za music-sharing-platform koristi json-server",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT --host 0.0.0.0 --cors"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "json-server": "^0.17.4"
  }
}
